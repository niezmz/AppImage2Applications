# AppImage2Applications 依赖文件

# 基础依赖（Python标准库，通常已包含）

# 必需依赖
pillow>=8.0.0  # 图标预览和图像处理功能


# 系统依赖
# python3-tkinter - GUI框架（需要系统安装）
# 在Ubuntu/Debian上运行: sudo apt-get install python3-tk
# 在Fedora上运行: sudo dnf install python3-tkinter
# 在Arch Linux上运行: sudo pacman -S tk 